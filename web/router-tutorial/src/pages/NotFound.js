const NotFound = () => {
  return (
    <div
      style={{
        display: "flex",
        alignItems: "cetner",
        justifyContent: "center",
        fontSize: 64,
        position: "absolute",
        width: "100%",
        height: "100%",
      }}
    >
      404
    </div>
  );
};

export default NotFound;
